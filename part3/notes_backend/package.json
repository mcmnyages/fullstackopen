{
  "name": "notes-backend",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "build:ui": "shx rm -rf dist && npm --prefix ../../part2/notes run build && shx cp -r ../../part2/notes/dist .",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "lint": "eslint"
  },
  "author": "@McMnyages",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.8.1",
    "globals": "^17.9.0",
    "shx": "^0.4.0"
  }
}
