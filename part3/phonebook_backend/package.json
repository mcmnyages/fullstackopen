{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build:ui": "shx rm -rf dist && npm --prefix ../../part2/phonebook run build && shx cp -r ../../part2/phonebook/dist .",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "dev": "node --watch index.js",
    "lint": "eslint ."
  },
  "author": "@McMnyages",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.1",
    "morgan": "^1.11.0",
    "shx": "^0.4.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.8.1",
    "globals": "^17.10.0"
  }
}
