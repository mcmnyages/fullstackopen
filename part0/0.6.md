```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes note and clicks Save

    Note right of browser: JavaScript prevents page reload

    Note right of browser: New note added to page instantly

    browser->>server: POST /new_note_spa
    activate server

    Note right of server: Server stores note

    server-->>browser: 201 Created
    deactivate server

    Note right of browser: No page reload occurs
```